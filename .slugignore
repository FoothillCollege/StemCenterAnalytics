# exclusion of files unnecessary to running web app on heroku (reduces slug size)
# these files/folders are ignored provide a further filter AFTER gitignore

# documentation
/docs
README.md

# tests
/tests

# scripts (only those that won't be used via chron jobs on heroku
/scripts/generate_demo_data.py

# specific modules
# [none yet]
