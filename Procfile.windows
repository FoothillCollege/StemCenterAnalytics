# to run locally on windows:
# heroku local -f Procfile.windows
#python stem_center_analytics.app.py
python -m stem_center_analytics.app

# HOWEVER - when gunicorn support does happen (it's on their agenda)
# the routine will change from pure python to the below:
# web: python stem_center_analytics.app.py runserver 0.0.0.0:33507
